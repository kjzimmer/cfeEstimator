# Company Info

- Sections: Identity, Products & Services, Assets, Employee Base, Service Rates, Material Costs
- No separate "job history" section — historical job data lives in Projects instead (see `projects.md`)
- Each section: a named text/markdown block, editable by any logged-in user
- This phase: populate Identity with real CFE content. Other sections show as real nav entries with an empty-state message ("not yet configured — coming soon") — visible roadmap, not hidden scope.
