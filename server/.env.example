# Postgres connection (Railway provides this in production)
DATABASE_URL=postgres://user:password@localhost:5432/cfe_estimator

# JWT signing secret
JWT_SECRET=change-me-in-production

# Anthropic API key — server-side only, never exposed to the client
ANTHROPIC_API_KEY=

# Port the Express server listens on
PORT=3001
